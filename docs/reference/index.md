# API Reference

Auto-generated from source docstrings. Each page corresponds to a module in `src/lib/`.

| Module | Purpose |
|--------|---------|
| `lib.utils.log` | structlog setup bridged through stdlib; `LIB_LOG_*` env vars |
| `lib.utils.misc` | `find_project_root()` helper |
